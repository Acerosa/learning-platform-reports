/**
 * Pilot interpretation thresholds for activity-level strengths/areas to review.
 *
 * Taken from `@learning-platform/results` `buildDiagnostics` defaults
 * (strengthThreshold = 80, weaknessThreshold = 50). These are formative
 * interpretation bands for the Reports pilot — not official college grade boundaries.
 */
export const REPORT_INTERPRETATION = Object.freeze({
  strengthThreshold: 80,
  weaknessThreshold: 50,
  source: "@learning-platform/results buildDiagnostics defaults"
});
